<UserControl x:Class="WarehouseInventory.Desktop.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="520"
             d:DesignWidth="540">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="34"
                       FontWeight="Bold"
                       Text="Регистрация" />
            <TextBlock Margin="0,10,0,0"
                       FontSize="16"
                       Foreground="#6D7480"
                       TextWrapping="Wrap">
                Создаем аккаунт через API. После успешной регистрации клиент сразу получает токен и открывает рабочую область.
            </TextBlock>
        </StackPanel>

        <Grid Grid.Row="1"
              Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Margin="0,0,12,0">
                <TextBlock FontSize="14"
                           FontWeight="SemiBold"
                           Text="Имя пользователя" />
                <TextBox Margin="0,8,0,0"
                         Height="42"
                         Padding="12,8"
                         Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Grid.Column="1"
                        Margin="12,0,0,0">
                <TextBlock FontSize="14"
                           FontWeight="SemiBold"
                           Text="ФИО сотрудника" />
                <TextBox Margin="0,8,0,0"
                         Height="42"
                         Padding="12,8"
                         Text="{Binding EmployeeFullName, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Grid.Row="1"
                        Margin="0,16,12,0">
                <TextBlock FontSize="14"
                           FontWeight="SemiBold"
                           Text="Email" />
                <TextBox Margin="0,8,0,0"
                         Height="42"
                         Padding="12,8"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Grid.Row="1"
                        Grid.Column="1"
                        Margin="12,16,0,0">
                <TextBlock FontSize="14"
                           FontWeight="SemiBold"
                           Text="Телефон" />
                <TextBox Margin="0,8,0,0"
                         Height="42"
                         Padding="12,8"
                         Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Grid.Row="2"
                        Margin="0,16,12,0">
                <TextBlock FontSize="14"
                           FontWeight="SemiBold"
                           Text="Пароль" />
                <TextBox Margin="0,8,0,0"
                         Height="42"
                         Padding="12,8"
                         Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Grid.Row="2"
                        Grid.Column="1"
                        Margin="12,16,0,0">
                <TextBlock FontSize="14"
                           FontWeight="SemiBold"
                           Text="Повтор пароля" />
                <TextBox Margin="0,8,0,0"
                         Height="42"
                         Padding="12,8"
                         Text="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <Border Grid.Row="3"
                    Grid.ColumnSpan="2"
                    Margin="0,22,0,0"
                    Padding="16"
                    CornerRadius="16"
                    Background="#F3EFFF">
                <TextBlock Foreground="#7A3FB0"
                           TextWrapping="Wrap"
                           Text="{Binding StatusMessage}" />
            </Border>
        </Grid>

        <StackPanel Grid.Row="2">
            <Button Height="50"
                    Background="#3756CE"
                    Foreground="White"
                    Command="{Binding SubmitCommand}"
                    Content="Зарегистрироваться" />
            <Button Margin="0,14,0,0"
                    Height="46"
                    Background="#00FFFFFF"
                    BorderThickness="1.5"
                    BorderBrush="#8D49C4"
                    Foreground="#8D49C4"
                    Command="{Binding OpenLoginCommand}"
                    Content="Уже есть аккаунт? Вход" />
        </StackPanel>
    </Grid>
</UserControl>
