<UserControl x:Class="WarehouseInventory.Desktop.Views.DocumentsSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="760"
             d:DesignWidth="1040">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="32" FontWeight="Bold" Text="{Binding Header}" />
            <TextBlock Margin="0,8,0,0" FontSize="15" Foreground="#667085" Text="{Binding Description}" />
        </StackPanel>

        <DockPanel Grid.Row="1" Margin="0,24,0,18" LastChildFill="False">
            <TextBlock VerticalAlignment="Center"
                       Foreground="#3756CE"
                       FontSize="14"
                       Text="{Binding StatusMessage}" />
            <Button DockPanel.Dock="Right"
                    Width="170"
                    Height="40"
                    Background="#FFCC00"
                    BorderThickness="0"
                    Command="{Binding DownloadReportCommand}"
                    Content="Скачать отчет" />
        </DockPanel>

        <UniformGrid Grid.Row="2" Columns="3" Rows="2">
            <Border Margin="0,0,14,14" Padding="14" CornerRadius="18" Background="#FFFFFF">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Foreground="#667085" FontSize="12" Text="Проверено" />
                    <TextBlock Margin="0,8,0,0" FontSize="26" FontWeight="Bold" Text="{Binding CheckedCount}" />
                </StackPanel>
            </Border>

            <Border Margin="0,0,14,14" Padding="14" CornerRadius="18" Background="#FFFFFF">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Foreground="#667085" FontSize="12" Text="Несоответствий" />
                    <TextBlock Margin="0,8,0,0" FontSize="26" FontWeight="Bold" Text="{Binding MismatchesCount}" />
                </StackPanel>
            </Border>

            <Border Margin="0,0,0,14" Padding="14" CornerRadius="18" Background="#FFFFFF">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Foreground="#667085" FontSize="12" Text="Сохранено" />
                    <TextBlock Margin="0,8,0,0" FontSize="18" FontWeight="SemiBold" Text="{Binding LastSavedText}" />
                </StackPanel>
            </Border>

            <Border Margin="0,0,14,0" Padding="14" CornerRadius="18" Background="#EAF7EE">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Foreground="#3C6E47" FontSize="12" Text="Совпадает" />
                    <TextBlock Margin="0,8,0,0" FontSize="26" FontWeight="Bold" Text="{Binding MatchesCount}" />
                </StackPanel>
            </Border>

            <Border Margin="0,0,14,0" Padding="14" CornerRadius="18" Background="#FFF3DB">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Foreground="#A66200" FontSize="12" Text="Недостача" />
                    <TextBlock Margin="0,8,0,0" FontSize="26" FontWeight="Bold" Text="{Binding ShortagesCount}" />
                </StackPanel>
            </Border>

            <Border Padding="14" CornerRadius="18" Background="#FDE7E7">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Foreground="#B42318" FontSize="12" Text="Избыток" />
                    <TextBlock Margin="0,8,0,0" FontSize="26" FontWeight="Bold" Text="{Binding SurplusCount}" />
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>
