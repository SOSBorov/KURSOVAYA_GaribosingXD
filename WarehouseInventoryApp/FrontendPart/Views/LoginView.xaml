<UserControl x:Class="WarehouseInventory.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="520"
             d:DesignWidth="540">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="34"
                       FontWeight="Bold"
                       Text="Вход" />
            <TextBlock Margin="0,10,0,0"
                       FontSize="16"
                       Foreground="#6D7480"
                       TextWrapping="Wrap">
                Авторизация desktop-приложения через WarehouseInventory API. После входа клиент сохраняет токен и открывает рабочую область.
            </TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Margin="0,28,0,0">
            <TextBlock FontSize="14"
                       FontWeight="SemiBold"
                       Text="Email" />
            <TextBox Margin="0,8,0,0"
                     Height="44"
                     Padding="12,8"
                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Margin="0,18,0,0"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Text="Пароль" />
            <TextBox Margin="0,8,0,0"
                     Height="44"
                     Padding="12,8"
                     Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" />

            <Border Margin="0,22,0,0"
                    Padding="16"
                    CornerRadius="16"
                    Background="#EEF2FF">
                <TextBlock Foreground="#3756CE"
                           TextWrapping="Wrap"
                           Text="{Binding StatusMessage}" />
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Button Height="50"
                    Background="#FFCC00"
                    Command="{Binding SubmitCommand}"
                    Content="Войти" />
            <Button Margin="0,14,0,0"
                    Height="46"
                    Background="#00FFFFFF"
                    BorderThickness="1.5"
                    BorderBrush="#3756CE"
                    Foreground="#3756CE"
                    Command="{Binding OpenRegisterCommand}"
                    Content="Нет аккаунта? Регистрация" />
        </StackPanel>
    </Grid>
</UserControl>
